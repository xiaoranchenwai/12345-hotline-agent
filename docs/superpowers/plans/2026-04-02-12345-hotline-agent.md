# 12345 Hotline Intelligent Customer Service Agent Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a multi-agent customer service system for the 12345 government hotline that handles complaints, inquiries, and suggestions using the DeepAgents (LangGraph) framework with local LLM support.

**Architecture:** A Supervisor Agent classifies user intent and routes to three sub-agents (Complaint, Inquiry, Suggestion). The Complaint Agent delegates to an Element Extraction Agent for structured element analysis. A session stack manager enables mid-conversation context switching (inquiry interrupts, multiple complaints). All agents use AGENTS.md for identity and SKILL.md files for specialized workflows.

**Tech Stack:** Python 3.11+, deepagents, langchain, langgraph, httpx, FastAPI (uvicorn), python-dotenv, rich

---

## File Structure

```
12345-hotline-agent/
├── pyproject.toml                              # Project dependencies
├── .env.example                                # Environment variable template
├── main.py                                     # Entry point: CLI + FastAPI web server
├── AGENTS.md                                   # Supervisor agent identity/instructions
├── agents/
│   ├── complaint/AGENTS.md                     # Complaint sub-agent instructions
│   ├── element_extraction/AGENTS.md            # Element extraction sub-agent instructions
│   ├── inquiry/AGENTS.md                       # Inquiry sub-agent instructions
│   └── suggestion/AGENTS.md                    # Suggestion sub-agent instructions
├── skills/
│   ├── intent-classification/SKILL.md          # Intent classification workflow
│   ├── complaint-type-mapping/SKILL.md         # Complaint type + element schema lookup
│   ├── knowledge-base-query/SKILL.md           # Knowledge base query + fallback
│   ├── empathy-response/SKILL.md               # Empathy/emotion handling
│   ├── ticket-submission/SKILL.md              # Ticket submission workflow
│   ├── fallback-handling/SKILL.md              # Error/exception fallback
│   ├── context-switch/SKILL.md                 # Mid-conversation task switching
│   ├── multi-complaint/SKILL.md                # Multiple complaint handling
│   └── warm-response-generation/SKILL.md       # Warm response for suggestions
├── data/
│   ├── complaint_schemas.json                  # Complaint type → required/optional elements
│   └── intent_keywords.json                    # Intent classification keyword dictionary
├── tools/
│   ├── __init__.py                             # Exports all tool functions
│   ├── knowledge_base.py                       # Knowledge base API client
│   ├── ticket_api.py                           # Ticket system API client
│   └── session_store.py                        # SessionStackManager for multi-task state
├── tests/
│   ├── __init__.py
│   ├── test_session_store.py                   # Tests for SessionStackManager
│   ├── test_tools.py                           # Tests for knowledge_base + ticket_api
│   └── test_agent_creation.py                  # Tests for agent factory functions
├── sessions/                                   # Runtime: session state files
└── logs/                                       # Runtime: log files
```

---

### Task 1: Project Scaffold — pyproject.toml, .env.example, directories

**Files:**
- Create: `pyproject.toml`
- Create: `.env.example`
- Create: `tools/__init__.py`
- Create: `tests/__init__.py`

- [ ] **Step 1: Create pyproject.toml**

```toml
[project]
name = "hotline-agent-12345"
version = "0.1.0"
description = "12345 政务热线智能客服系统 - 基于 DeepAgents 框架"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.3.5",
    "langchain>=1.2.3",
    "langchain-community>=0.3.0",
    "langgraph>=1.0.6",
    "httpx>=0.27.0",
    "python-dotenv>=1.0.0",
    "rich>=13.0.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
```

- [ ] **Step 2: Create .env.example**

```bash
# 本地大模型服务
LLM_BASE_URL=http://localhost:8000/v1
LLM_MODEL_NAME=your-local-model-name
LLM_API_KEY=none

# 知识库服务
KNOWLEDGE_BASE_URL=http://your-kb-service/api/query
KNOWLEDGE_BASE_TOKEN=your_token

# 工单系统
TICKET_API_URL=http://your-ticket-system/api/tickets
TICKET_API_KEY=your_ticket_key

# 会话存储路径
SESSION_DIR=./sessions
LOG_DIR=./logs

# （可选）LangSmith 链路追踪
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=12345-hotline-agent
```

- [ ] **Step 3: Create empty __init__.py files and runtime directories**

```bash
mkdir -p agents/complaint agents/element_extraction agents/inquiry agents/suggestion
mkdir -p skills/intent-classification skills/complaint-type-mapping skills/knowledge-base-query
mkdir -p skills/empathy-response skills/ticket-submission skills/fallback-handling
mkdir -p skills/context-switch skills/multi-complaint skills/warm-response-generation
mkdir -p data tools tests sessions logs
touch tools/__init__.py tests/__init__.py
```

- [ ] **Step 4: Commit**

```bash
git add pyproject.toml .env.example tools/__init__.py tests/__init__.py
git commit -m "chore: project scaffold with dependencies and directory structure"
```

---

### Task 2: Data Files — complaint_schemas.json and intent_keywords.json

**Files:**
- Create: `data/complaint_schemas.json`
- Create: `data/intent_keywords.json`

- [ ] **Step 1: Create complaint_schemas.json**

```json
{
  "WATER_OUTAGE": {
    "display_name": "停水投诉",
    "required_elements": [
      {"key": "address", "label": "详细地址", "question": "请问您的详细地址是（精确到楼栋）？", "validation": "非空"},
      {"key": "start_time", "label": "停水开始时间", "question": "请问大概是从什么时间开始停水的？", "validation": "时间格式"},
      {"key": "is_restored", "label": "是否已恢复", "question": "请问目前水已经恢复了吗？", "validation": "布尔"}
    ],
    "optional_elements": [
      {"key": "contact_phone", "label": "联系电话", "question": "方便留一个联系电话，以便后续跟进吗？"},
      {"key": "affected_scope", "label": "影响范围", "question": "请问停水影响的范围大概有多大（如整栋楼、整个小区）？"}
    ]
  },
  "POWER_OUTAGE": {
    "display_name": "停电投诉",
    "required_elements": [
      {"key": "address", "label": "详细地址", "question": "请问您的详细地址是？", "validation": "非空"},
      {"key": "start_time", "label": "停电开始时间", "question": "请问大概是从什么时间开始停电的？", "validation": "时间格式"},
      {"key": "is_whole_building", "label": "是否整栋楼停电", "question": "请问是整栋楼都停电还是只有您家？", "validation": "布尔"}
    ],
    "optional_elements": [
      {"key": "contact_phone", "label": "联系电话", "question": "方便留一个联系电话吗？"},
      {"key": "equipment_damage", "label": "设备损坏情况", "question": "停电期间有没有造成电器损坏？"}
    ]
  },
  "NOISE_COMPLAINT": {
    "display_name": "噪音扰民投诉",
    "required_elements": [
      {"key": "noise_location", "label": "噪音来源位置", "question": "请问噪音是从哪里发出来的？", "validation": "非空"},
      {"key": "time_period", "label": "发生时间段", "question": "请问噪音主要在什么时间段发生？", "validation": "非空"},
      {"key": "noise_type", "label": "噪音类型", "question": "请问噪音属于哪种类型（施工、娱乐、生活等）？", "validation": "枚举:施工,娱乐,生活,其他"}
    ],
    "optional_elements": [
      {"key": "frequency", "label": "频次", "question": "这种噪音大概多久出现一次？"},
      {"key": "self_negotiation", "label": "已协商记录", "question": "请问您是否已经尝试自行与对方协商？"}
    ]
  },
  "ROAD_DAMAGE": {
    "display_name": "道路损坏投诉",
    "required_elements": [
      {"key": "road_location", "label": "具体路段或路口", "question": "请问具体是哪条路或哪个路口？", "validation": "非空"},
      {"key": "damage_type", "label": "损坏类型", "question": "请问损坏类型是什么（坑洞、护栏损坏、标线不清等）？", "validation": "枚举:坑洞,护栏损坏,标线不清,其他"},
      {"key": "affects_traffic", "label": "是否影响通行", "question": "请问目前是否影响正常通行？", "validation": "布尔"}
    ],
    "optional_elements": [
      {"key": "photo_description", "label": "照片描述", "question": "如果方便的话，能描述一下现场情况吗？"},
      {"key": "discovery_time", "label": "发现时间", "question": "请问您是什么时候发现这个问题的？"}
    ]
  },
  "GAS_ISSUE": {
    "display_name": "燃气问题投诉",
    "required_elements": [
      {"key": "address", "label": "详细地址", "question": "请问您的详细地址是？", "validation": "非空"},
      {"key": "issue_type", "label": "问题类型", "question": "请问具体是什么燃气问题（停气、泄漏、设备故障等）？", "validation": "枚举:停气,泄漏,设备故障,其他"},
      {"key": "start_time", "label": "问题开始时间", "question": "请问问题大概是从什么时候开始的？", "validation": "时间格式"}
    ],
    "optional_elements": [
      {"key": "contact_phone", "label": "联系电话", "question": "方便留一个联系电话吗？"},
      {"key": "safety_concern", "label": "安全隐患", "question": "目前是否有安全隐患需要紧急处理？"}
    ]
  },
  "OTHER": {
    "display_name": "其他投诉",
    "required_elements": [
      {"key": "description", "label": "问题描述", "question": "请您详细描述一下遇到的问题。", "validation": "非空"},
      {"key": "address", "label": "相关地址", "question": "请问问题发生的具体地点在哪里？", "validation": "非空"},
      {"key": "start_time", "label": "发生时间", "question": "请问问题是什么时候发生的？", "validation": "时间格式"}
    ],
    "optional_elements": [
      {"key": "contact_phone", "label": "联系电话", "question": "方便留一个联系电话吗？"}
    ]
  }
}
```

- [ ] **Step 2: Create intent_keywords.json**

```json
{
  "COMPLAINT": {
    "keywords": ["投诉", "举报", "不满", "问题", "损坏", "停水", "停电", "停气", "噪音", "扰民", "坏了", "不行了", "没有水", "没有电", "臭", "脏", "乱", "堵", "漏水", "漏气"],
    "sentiment_indicators": ["烦", "气死", "受不了", "太过分", "不像话", "敷衍", "没人管"]
  },
  "INQUIRY": {
    "keywords": ["怎么办", "如何", "流程", "政策", "查询", "需要什么", "在哪里", "什么时候", "多少钱", "办理", "申请", "材料", "条件", "规定", "标准", "手续"],
    "sentiment_indicators": []
  },
  "SUGGESTION": {
    "keywords": ["建议", "希望", "改进", "能不能", "应该", "最好", "可以增加", "可以改善", "期望", "盼望", "提议"],
    "sentiment_indicators": []
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add data/
git commit -m "feat: add complaint schemas and intent keywords data files"
```

---

### Task 3: Session Store — SessionStackManager

**Files:**
- Create: `tools/session_store.py`
- Create: `tests/test_session_store.py`

- [ ] **Step 1: Write failing tests for SessionStackManager**

```python
# tests/test_session_store.py
import json
import pytest
from pathlib import Path
from tools.session_store import SessionStackManager


@pytest.fixture
def tmp_session_dir(tmp_path):
    return str(tmp_path / "sessions")


@pytest.fixture
def manager(tmp_session_dir):
    return SessionStackManager("test-session-001", session_dir=tmp_session_dir)


def test_init_creates_empty_state(manager):
    assert manager.state["session_id"] == "test-session-001"
    assert manager.state["active_task"] is None
    assert manager.state["task_stack"] == []
    assert manager.state["completed_tasks"] == []
    assert manager.state["interrupt_depth"] == 0


def test_can_interrupt_initially_true(manager):
    assert manager.can_interrupt() is True


def test_pause_active_and_push_sets_new_task_active(manager):
    # First set an active task
    manager.state["active_task"] = {
        "task_id": "complaint-001",
        "agent_type": "COMPLAINT",
        "complaint_type": "WATER_OUTAGE",
        "collected": {"address": "XX路88号"},
        "missing": ["start_time"],
        "next_question": "请问什么时间开始停水？",
        "status": "ACTIVE",
    }

    new_task = {
        "task_id": "inquiry-001",
        "agent_type": "INQUIRY",
        "query": "临时供水怎么申请",
    }
    result = manager.pause_active_and_push(new_task)

    assert result is True
    assert manager.state["active_task"]["task_id"] == "inquiry-001"
    assert manager.state["active_task"]["status"] == "ACTIVE"
    assert len(manager.state["task_stack"]) == 1
    assert manager.state["task_stack"][0]["status"] == "PAUSED"
    assert manager.state["interrupt_depth"] == 1


def test_queue_pending_keeps_current_active(manager):
    manager.state["active_task"] = {
        "task_id": "complaint-001",
        "agent_type": "COMPLAINT",
        "status": "ACTIVE",
    }
    new_complaint = {
        "task_id": "complaint-002",
        "agent_type": "COMPLAINT",
        "complaint_type": "FACILITY_DAMAGE",
    }
    result = manager.queue_pending(new_complaint)

    assert result is True
    assert manager.state["active_task"]["task_id"] == "complaint-001"
    assert manager.state["active_task"]["status"] == "ACTIVE"
    assert manager.state["task_stack"][0]["task_id"] == "complaint-002"
    assert manager.state["task_stack"][0]["status"] == "PENDING"


def test_complete_active_and_resume_returns_next_task(manager):
    manager.state["active_task"] = {
        "task_id": "inquiry-001",
        "agent_type": "INQUIRY",
        "status": "ACTIVE",
    }
    manager.state["task_stack"] = [
        {
            "task_id": "complaint-001",
            "agent_type": "COMPLAINT",
            "status": "PAUSED",
            "next_question": "请问什么时间？",
        }
    ]
    manager.state["interrupt_depth"] = 1

    next_task = manager.complete_active_and_resume(ticket_id=None)

    assert next_task is not None
    assert next_task["task_id"] == "complaint-001"
    assert next_task["status"] == "ACTIVE"
    assert len(manager.state["completed_tasks"]) == 1
    assert manager.state["interrupt_depth"] == 0


def test_complete_active_returns_none_when_stack_empty(manager):
    manager.state["active_task"] = {
        "task_id": "complaint-001",
        "agent_type": "COMPLAINT",
        "status": "ACTIVE",
    }
    next_task = manager.complete_active_and_resume(ticket_id="T-001")
    assert next_task is None
    assert len(manager.state["completed_tasks"]) == 1
    assert manager.state["completed_tasks"][0]["ticket_id"] == "T-001"


def test_interrupt_depth_limit(manager):
    manager.state["interrupt_depth"] = 2
    assert manager.can_interrupt() is False

    result = manager.pause_active_and_push({"task_id": "x", "agent_type": "INQUIRY"})
    assert result is False


def test_persistence(tmp_session_dir):
    m1 = SessionStackManager("persist-test", session_dir=tmp_session_dir)
    m1.state["active_task"] = {"task_id": "t1", "agent_type": "COMPLAINT", "status": "ACTIVE"}
    m1._save()

    m2 = SessionStackManager("persist-test", session_dir=tmp_session_dir)
    assert m2.state["active_task"]["task_id"] == "t1"


def test_get_resume_transition_inquiry(manager):
    completed = {"agent_type": "INQUIRY", "query": "临时供水申请"}
    next_task = {"agent_type": "COMPLAINT", "next_question": "请问停水什么时间开始？"}
    text = manager.get_resume_transition(completed, next_task)
    assert "临时供水申请" in text
    assert "请问停水什么时间开始？" in text


def test_get_summary(manager):
    manager.state["completed_tasks"] = [
        {"agent_type": "COMPLAINT", "display_name": "停水投诉", "ticket_id": "A001"},
        {"agent_type": "COMPLAINT", "display_name": "停电投诉", "ticket_id": "A002"},
    ]
    summary = manager.get_summary()
    assert "A001" in summary
    assert "A002" in summary
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd /home/zhengxin/Documents/workspace/12345-hotline-agent
python -m pytest tests/test_session_store.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'tools.session_store'`

- [ ] **Step 3: Implement SessionStackManager**

```python
# tools/session_store.py
import json
from pathlib import Path
from datetime import datetime
from typing import Optional


class SessionStackManager:
    """管理会话中的多任务栈，持久化到文件系统"""

    def __init__(self, session_id: str, session_dir: str = "./sessions"):
        self.session_id = session_id
        self.session_dir = Path(session_dir)
        self.session_dir.mkdir(parents=True, exist_ok=True)
        self.session_file = self.session_dir / f"{session_id}.json"
        self.state = self._load_or_init()

    def _load_or_init(self) -> dict:
        if self.session_file.exists():
            return json.loads(self.session_file.read_text(encoding="utf-8"))
        return {
            "session_id": self.session_id,
            "active_task": None,
            "task_stack": [],
            "completed_tasks": [],
            "interrupt_depth": 0,
            "last_updated": datetime.now().isoformat(),
        }

    def _save(self):
        self.state["last_updated"] = datetime.now().isoformat()
        self.session_file.write_text(
            json.dumps(self.state, ensure_ascii=False, indent=2),
            encoding="utf-8",
        )

    def can_interrupt(self) -> bool:
        return self.state["interrupt_depth"] < 2

    def pause_active_and_push(self, new_task: dict) -> bool:
        """暂停当前任务，将新任务设为活跃（用于咨询/建议打断投诉）"""
        if not self.can_interrupt():
            return False
        if self.state["active_task"]:
            self.state["active_task"]["status"] = "PAUSED"
            self.state["task_stack"].append(self.state["active_task"])
        new_task["status"] = "ACTIVE"
        new_task["created_reason"] = "USER_INTERRUPT"
        self.state["active_task"] = new_task
        self.state["interrupt_depth"] += 1
        self._save()
        return True

    def queue_pending(self, new_task: dict) -> bool:
        """保持当前任务活跃，新投诉入队（用于投诉中新增投诉）"""
        if not self.can_interrupt():
            return False
        new_task["status"] = "PENDING"
        new_task["created_reason"] = "USER_INTERRUPT"
        self.state["task_stack"].append(new_task)
        self.state["interrupt_depth"] += 1
        self._save()
        return True

    def complete_active_and_resume(self, ticket_id: Optional[str] = None) -> Optional[dict]:
        """完成当前任务，从栈中恢复下一个"""
        if self.state["active_task"]:
            completed = self.state["active_task"]
            completed["status"] = "COMPLETED"
            if ticket_id:
                completed["ticket_id"] = ticket_id
            self.state["completed_tasks"].append(completed)
            self.state["active_task"] = None

        if self.state["task_stack"]:
            next_task = self.state["task_stack"].pop()
            next_task["status"] = "ACTIVE"
            self.state["active_task"] = next_task
            self.state["interrupt_depth"] = max(0, self.state["interrupt_depth"] - 1)
            self._save()
            return next_task

        self._save()
        return None

    def get_resume_transition(self, completed_task: dict, next_task: dict) -> str:
        """生成任务切换的自然过渡话术"""
        type_labels = {"INQUIRY": "咨询", "COMPLAINT": "投诉", "SUGGESTION": "建议"}
        next_label = type_labels.get(next_task.get("agent_type", ""), "问题")
        next_q = next_task.get("next_question", "")

        if completed_task.get("agent_type") == "INQUIRY":
            query = completed_task.get("query", "您咨询的问题")
            return f"好的，以上是关于"{query}"的解答。我们继续刚才的{next_label}——{next_q}"

        if completed_task.get("agent_type") == "COMPLAINT":
            tid = completed_task.get("ticket_id", "处理中")
            name = completed_task.get("display_name", "投诉")
            return f"{name}已为您登记（工单号：{tid}）。接下来我们处理{next_label}的问题——{next_q}"

        c_label = type_labels.get(completed_task.get("agent_type", ""), "问题")
        return f"好的，{c_label}已处理完毕。我们继续——{next_q}"

    def get_summary(self) -> str:
        """生成所有已完成任务的汇总"""
        tasks = self.state["completed_tasks"]
        if not tasks:
            return ""
        lines = [f"您本次共处理了 {len(tasks)} 项事务："]
        for i, t in enumerate(tasks, 1):
            name = t.get("display_name", t.get("agent_type", "事务"))
            tid = t.get("ticket_id")
            if tid:
                lines.append(f"  {i}. {name}（工单号：{tid}）")
            else:
                lines.append(f"  {i}. {name}（已完成）")
        return "\n".join(lines)
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
python -m pytest tests/test_session_store.py -v
```

Expected: All PASS

- [ ] **Step 5: Commit**

```bash
git add tools/session_store.py tests/test_session_store.py
git commit -m "feat: add SessionStackManager with multi-task stack and persistence"
```

---

### Task 4: Tools — Knowledge Base and Ticket API clients

**Files:**
- Create: `tools/knowledge_base.py`
- Create: `tools/ticket_api.py`
- Create: `tests/test_tools.py`

- [ ] **Step 1: Write failing tests**

```python
# tests/test_tools.py
import json
import pytest
from unittest.mock import AsyncMock, patch, MagicMock
from tools.knowledge_base import query_knowledge_base
from tools.ticket_api import submit_ticket


def test_query_knowledge_base_returns_string():
    """query_knowledge_base should return a JSON string for LangChain tool compatibility"""
    with patch("tools.knowledge_base.httpx") as mock_httpx:
        mock_response = MagicMock()
        mock_response.status_code = 200
        mock_response.json.return_value = {"found": True, "answer": "带身份证即可", "source": "kb"}
        mock_response.raise_for_status = MagicMock()
        mock_client = MagicMock()
        mock_client.__enter__ = MagicMock(return_value=mock_client)
        mock_client.__exit__ = MagicMock(return_value=False)
        mock_client.post.return_value = mock_response
        mock_httpx.Client.return_value = mock_client

        result = query_knowledge_base("办理营业执照需要什么材料")
        parsed = json.loads(result)
        assert parsed["found"] is True
        assert "身份证" in parsed["answer"]


def test_query_knowledge_base_timeout_fallback():
    """On timeout, should return found=False with source=timeout"""
    with patch("tools.knowledge_base.httpx") as mock_httpx:
        import httpx as real_httpx
        mock_client = MagicMock()
        mock_client.__enter__ = MagicMock(return_value=mock_client)
        mock_client.__exit__ = MagicMock(return_value=False)
        mock_client.post.side_effect = real_httpx.TimeoutException("timeout")
        mock_httpx.Client.return_value = mock_client
        mock_httpx.TimeoutException = real_httpx.TimeoutException

        result = query_knowledge_base("test")
        parsed = json.loads(result)
        assert parsed["found"] is False
        assert parsed["source"] == "timeout"


def test_submit_ticket_returns_ticket_id():
    """submit_ticket should return JSON with ticket_id"""
    with patch("tools.ticket_api.httpx") as mock_httpx:
        mock_response = MagicMock()
        mock_response.status_code = 200
        mock_response.json.return_value = {"ticket_id": "20240115-00234", "status": "created"}
        mock_response.raise_for_status = MagicMock()
        mock_client = MagicMock()
        mock_client.__enter__ = MagicMock(return_value=mock_client)
        mock_client.__exit__ = MagicMock(return_value=False)
        mock_client.post.return_value = mock_response
        mock_httpx.Client.return_value = mock_client

        result = submit_ticket(
            complaint_type="WATER_OUTAGE",
            elements={"address": "XX路88号", "start_time": "昨晚8点", "is_restored": "否"},
            session_id="test-001",
        )
        parsed = json.loads(result)
        assert parsed["success"] is True
        assert parsed["ticket_id"] == "20240115-00234"


def test_submit_ticket_failure_saves_locally(tmp_path):
    """On API failure, should save ticket locally and return success=False"""
    with patch("tools.ticket_api.httpx") as mock_httpx:
        mock_client = MagicMock()
        mock_client.__enter__ = MagicMock(return_value=mock_client)
        mock_client.__exit__ = MagicMock(return_value=False)
        mock_client.post.side_effect = Exception("connection refused")
        mock_httpx.Client.return_value = mock_client

        with patch("tools.ticket_api.FALLBACK_DIR", str(tmp_path)):
            result = submit_ticket(
                complaint_type="WATER_OUTAGE",
                elements={"address": "XX路88号"},
                session_id="test-002",
            )
            parsed = json.loads(result)
            assert parsed["success"] is False
            assert parsed["saved_locally"] is True
            # Check file was saved
            saved_files = list(tmp_path.glob("*.json"))
            assert len(saved_files) == 1
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
python -m pytest tests/test_tools.py -v
```

Expected: FAIL — `ModuleNotFoundError`

- [ ] **Step 3: Implement knowledge_base.py**

```python
# tools/knowledge_base.py
"""知识库 API 调用工具 — 封装为 LangChain tool 可用的同步函数"""

import json
import os

import httpx
from langchain_core.tools import tool


KNOWLEDGE_BASE_URL = os.getenv("KNOWLEDGE_BASE_URL", "http://localhost:8080/api/query")
KNOWLEDGE_BASE_TOKEN = os.getenv("KNOWLEDGE_BASE_TOKEN", "")


@tool
def query_knowledge_base(query: str, category: str = "") -> str:
    """调用政务知识库接口检索答案。参数 query 为用户的咨询问题关键词，category 可选分类过滤。
    返回 JSON 字符串，包含 found(bool), answer(str|null), source(str)。"""
    try:
        with httpx.Client(timeout=3.0) as client:
            headers = {}
            if KNOWLEDGE_BASE_TOKEN:
                headers["Authorization"] = f"Bearer {KNOWLEDGE_BASE_TOKEN}"
            response = client.post(
                KNOWLEDGE_BASE_URL,
                json={"query": query, "category": category or None},
                headers=headers,
            )
            response.raise_for_status()
            return json.dumps(response.json(), ensure_ascii=False)
    except httpx.TimeoutException:
        return json.dumps({"found": False, "answer": None, "source": "timeout"}, ensure_ascii=False)
    except Exception as e:
        return json.dumps({"found": False, "answer": None, "source": f"error: {str(e)}"}, ensure_ascii=False)
```

- [ ] **Step 4: Implement ticket_api.py**

```python
# tools/ticket_api.py
"""工单系统 API 调用工具"""

import json
import os
from datetime import datetime
from pathlib import Path

import httpx
from langchain_core.tools import tool


TICKET_API_URL = os.getenv("TICKET_API_URL", "http://localhost:8080/api/tickets")
TICKET_API_KEY = os.getenv("TICKET_API_KEY", "")
FALLBACK_DIR = os.getenv("SESSION_DIR", "./sessions")


@tool
def submit_ticket(complaint_type: str, elements: dict, session_id: str = "") -> str:
    """提交投诉工单到业务系统。参数 complaint_type 为投诉类型代码，elements 为已收集的要素字典，
    session_id 为会话标识。返回 JSON 字符串，包含 success(bool), ticket_id(str|null)。"""
    payload = {
        "complaint_type": complaint_type,
        "elements": elements,
        "session_id": session_id,
        "submitted_at": datetime.now().isoformat(),
    }
    try:
        with httpx.Client(timeout=5.0) as client:
            headers = {}
            if TICKET_API_KEY:
                headers["Authorization"] = f"Bearer {TICKET_API_KEY}"
            response = client.post(TICKET_API_URL, json=payload, headers=headers)
            response.raise_for_status()
            data = response.json()
            return json.dumps(
                {"success": True, "ticket_id": data.get("ticket_id"), "saved_locally": False},
                ensure_ascii=False,
            )
    except Exception as e:
        # 降级：保存到本地文件
        fallback_path = Path(FALLBACK_DIR)
        fallback_path.mkdir(parents=True, exist_ok=True)
        filename = f"ticket_{session_id}_{datetime.now().strftime('%Y%m%d%H%M%S')}.json"
        (fallback_path / filename).write_text(
            json.dumps(payload, ensure_ascii=False, indent=2), encoding="utf-8"
        )
        return json.dumps(
            {"success": False, "ticket_id": None, "saved_locally": True, "error": str(e)},
            ensure_ascii=False,
        )
```

- [ ] **Step 5: Update tools/__init__.py**

```python
# tools/__init__.py
from tools.knowledge_base import query_knowledge_base
from tools.ticket_api import submit_ticket
from tools.session_store import SessionStackManager

__all__ = ["query_knowledge_base", "submit_ticket", "SessionStackManager"]
```

- [ ] **Step 6: Run tests to verify they pass**

```bash
python -m pytest tests/test_tools.py -v
```

Expected: All PASS

- [ ] **Step 7: Commit**

```bash
git add tools/ tests/test_tools.py
git commit -m "feat: add knowledge base and ticket API tool wrappers with fallback"
```

---

### Task 5: AGENTS.md Files — All Agent Identities

**Files:**
- Create: `AGENTS.md` (supervisor)
- Create: `agents/complaint/AGENTS.md`
- Create: `agents/element_extraction/AGENTS.md`
- Create: `agents/inquiry/AGENTS.md`
- Create: `agents/suggestion/AGENTS.md`

- [ ] **Step 1: Create supervisor AGENTS.md**

```markdown
# 12345 热线主智能体

## 角色定位
你是 12345 政务服务热线的智能客服主调度员，负责接待市民，判断其诉求类型，
并将对话路由到最合适的处理流程。

## 行为准则
- 语气：礼貌、温和、专业，使用普通话书面表达
- 每轮回复不超过 150 字，避免冗长
- 意图不明时最多追问一次，仍不明确则默认进入咨询流程
- 禁止承诺具体办理时限或结果

## 意图分类规则
- 包含"投诉""举报""不满""问题""损坏""停止服务"等词 → COMPLAINT
- 包含"怎么办""如何""流程""政策""查询""需要什么"等词 → INQUIRY
- 包含"建议""希望""改进""能不能""应该"等词 → SUGGESTION
- 歧义或混合意图 → 追问一次确认

## 中途切换处理规则

### 咨询插入投诉
- 检测时机：每次用户回复后，先判断是否包含与当前任务无关的新意图
- 判断标准：新意图置信度 > 0.75 且与当前任务类型不同
- 处理：调用 context-switch Skill，暂停当前任务，处理新意图，完成后自动续接
- 禁止：不得丢失任何已收集要素；不得要求用户重述投诉内容

### 投诉插入投诉
- 处理：调用 multi-complaint Skill，评估关联性后决定处理策略
- 优先级：当前任务优先完成，新投诉进入 PENDING 队列

### interrupt_depth 守卫
- interrupt_depth >= 2 时：拒绝新切换，引导用户按顺序处理
- 回复模板："我注意到您有几个问题要反映，我们逐一处理效率更高，请先告诉我 [当前问题的下一个追问]"

## 安全规则
- 不讨论与政务服务无关的话题
- 不评价政府工作的好坏
- 遇到投诉类语言攻击，保持克制并引导回正题
```

- [ ] **Step 2: Create complaint agent AGENTS.md**

```markdown
# 投诉处理子智能体

## 角色定位
你负责处理市民投诉，目标是高效、完整地收集投诉要素，
并生成准确的工单提交到处理系统。

## 收集策略
- 每次只问一个问题，不要一次性列出所有缺失要素
- 用户已提供的信息不重复询问
- 对用户的困扰表示理解，再继续收集
- 超过 3 次无法获取某一要素时，标记为"用户未提供"并继续

## 投诉类型识别
支持以下投诉子类型：
- WATER_OUTAGE（停水）
- POWER_OUTAGE（停电）
- NOISE_COMPLAINT（噪音扰民）
- ROAD_DAMAGE（道路损坏）
- GAS_ISSUE（燃气问题）
- OTHER（其他）

## 委托规则
- 每次用户回答后，调用要素提取子智能体分析当前要素完整度
- 根据提取结果决定下一步：继续追问 or 提交工单

## 工单提交规范
提交前向用户确认关键信息，格式：
"我已为您记录以下信息：[摘要]，是否确认提交？"

## 情绪处理
- 检测到用户情绪激动时，先共情安抚再继续收集
- 安抚话术示例："非常理解您的不便和困扰，我们一定认真记录并跟进您的问题。"
```

- [ ] **Step 3: Create element extraction agent AGENTS.md**

```markdown
# 要素提取分析子智能体

## 角色定位
你是一个信息分析专家，只负责分析对话内容并输出结构化的要素状态报告，
不直接与用户交互。

## 工作方式
1. 接收：投诉类型 + 必填要素清单 + 对话历史
2. 逐一核查每个要素是否已在对话中明确获取
3. 识别表达模糊的要素（如时间说"最近"、"前两天"）
4. 输出标准 JSON 格式报告

## 输出要求
- 必须输出合法 JSON，不输出任何额外文字
- completion_rate 为 0~1 的浮点数
- next_question 为建议的下一个追问语句（中文）
- is_complete 为 true 时表示可以提交工单

## 输出格式
```json
{
  "complaint_type": "投诉类型",
  "collected": {"key": "value"},
  "missing": ["key1", "key2"],
  "ambiguous": {"key": "模糊原因"},
  "next_question": "建议的下一个追问",
  "completion_rate": 0.67,
  "is_complete": false
}
```

## 分析规则
- "昨天""今早""刚才"等相对时间，标记为 ambiguous，建议确认具体时间
- "我家""这里"等模糊地址，标记为 ambiguous，建议确认详细地址
- 用户明确回答"不知道""不清楚"的要素，标记为 collected 但值为"用户未提供"
```

- [ ] **Step 4: Create inquiry agent AGENTS.md**

```markdown
# 咨询处理子智能体

## 角色定位
你负责处理政策法规、办事流程、服务查询等咨询类问题，
优先调用知识库接口获取权威答案。

## 查询策略
- 提取用户咨询的核心关键词，构造知识库查询请求
- 知识库有结果时：整理后以简洁语言向用户呈现，附上来源或办理入口
- 知识库无结果时：使用大模型通用知识回答，并明确告知"以官方最新规定为准"
- 知识库接口超时时：启用降级策略，使用大模型知识兜底并记录日志

## 答案格式
- 不超过 200 字
- 分步骤时使用数字序号
- 附上相关办理窗口或链接（如知识库有提供）

## 复杂咨询处理
对于涉及多个部门的复杂咨询，拆分问题分步解答。

## 免责声明
当使用大模型知识兜底时，必须附加声明：
"以上为参考信息，请以官方最新规定为准。如需进一步确认，建议拨打相关部门电话咨询。"
```

- [ ] **Step 5: Create suggestion agent AGENTS.md**

```markdown
# 建议处理子智能体

## 角色定位
你负责接收市民对城市治理、公共服务的改进建议，
通过理解和整理建议内容，给予积极回应并记录。

## 处理流程
1. 认真倾听并感谢市民提出建议
2. 对建议进行要点提炼和分类（城市建设/交通/环境/教育/医疗/其他）
3. 给予积极、有温度的回应，不做具体承诺但表达重视
4. 将整理后的建议摘要记录

## 回应准则
- 感谢用户的参与和关注
- 肯定建议的价值和意义
- 说明建议已被记录并将转达相关部门
- 不做具体承诺（如"一定会实施"）

## 意图纠正
若建议明显属于投诉或咨询，礼貌引导：
- "您反映的情况更像是一个需要处理的问题，我帮您转到投诉流程，这样处理会更快。"
- "您的问题我可以直接帮您查询，不需要以建议形式提交。"
```

- [ ] **Step 6: Commit**

```bash
git add AGENTS.md agents/
git commit -m "feat: add AGENTS.md for all five agents (supervisor, complaint, extraction, inquiry, suggestion)"
```

---

### Task 6: SKILL.md Files — All Skills

**Files:**
- Create: `skills/intent-classification/SKILL.md`
- Create: `skills/complaint-type-mapping/SKILL.md`
- Create: `skills/knowledge-base-query/SKILL.md`
- Create: `skills/empathy-response/SKILL.md`
- Create: `skills/ticket-submission/SKILL.md`
- Create: `skills/fallback-handling/SKILL.md`
- Create: `skills/context-switch/SKILL.md`
- Create: `skills/multi-complaint/SKILL.md`
- Create: `skills/warm-response-generation/SKILL.md`

- [ ] **Step 1: Create intent-classification SKILL.md**

```markdown
---
name: intent-classification
description: 对用户的自然语言输入进行三分类（投诉/咨询/建议），用于主智能体路由决策。
---

## 分类工作流

1. 提取用户输入中的关键词和情感倾向
2. 匹配分类规则（见 AGENTS.md 意图分类规则）
3. 置信度低于 0.7 时触发一次确认追问
4. 输出分类结果和置信度

## 输出格式
```json
{
  "intent": "COMPLAINT | INQUIRY | SUGGESTION",
  "confidence": 0.0~1.0,
  "keywords": ["关键词列表"],
  "need_clarification": true/false,
  "clarification_question": "追问内容（如需）"
}
```

## 分类参考词典
- COMPLAINT: 投诉、举报、不满、问题、损坏、停水、停电、噪音、扰民
- INQUIRY: 怎么办、如何、流程、政策、查询、需要什么、办理
- SUGGESTION: 建议、希望、改进、能不能、应该
```

- [ ] **Step 2: Create complaint-type-mapping SKILL.md**

```markdown
---
name: complaint-type-mapping
description: 根据投诉内容识别具体的投诉子类型，并返回对应的必填/选填要素清单。
---

## 支持的投诉子类型
- WATER_OUTAGE（停水）
- POWER_OUTAGE（停电）
- NOISE_COMPLAINT（噪音扰民）
- ROAD_DAMAGE（道路损坏）
- GAS_ISSUE（燃气问题）
- OTHER（其他）

## 工作流
1. 分析投诉描述，匹配最近似的子类型
2. 从 data/complaint_schemas.json 加载对应的要素清单
3. 返回结构化的要素需求对象

## 关键词到类型映射
- 停水/没水/水管/自来水 → WATER_OUTAGE
- 停电/没电/跳闸/电力 → POWER_OUTAGE
- 噪音/吵/扰民/施工噪声 → NOISE_COMPLAINT
- 路面/坑洞/道路/护栏/路灯 → ROAD_DAMAGE
- 燃气/天然气/煤气/停气 → GAS_ISSUE
- 以上均不匹配 → OTHER
```

- [ ] **Step 3: Create knowledge-base-query SKILL.md**

```markdown
---
name: knowledge-base-query
description: 调用政务知识库 API 检索权威答案，处理返回结果或触发降级策略。
---

## 查询工作流

1. 提取关键词，构造查询参数
2. 调用 query_knowledge_base 工具
3. 超时（>3s）→ 降级到大模型通用知识
4. 返回空 → 尝试拆分问题再次查询
5. 成功返回 → 格式化呈现给用户

## 降级策略
- 一级降级：拆分关键词重试
- 二级降级：使用大模型知识回答，添加免责声明
- 三级降级：提示用户拨打人工坐席

## 答案格式化要求
- 不超过 200 字
- 分步骤时使用数字序号
- 附上相关办理窗口或链接（如知识库有提供）
```

- [ ] **Step 4: Create empathy-response SKILL.md**

```markdown
---
name: empathy-response
description: 检测用户负面情绪并生成安抚话术，在信息收集前先进行情绪疏导。
---

## 触发条件
检测到强烈负面情绪词汇（愤怒、投诉无用、敷衍、没人管等）

## 处理流程
1. 识别用户的情绪状态和核心诉求
2. 生成安抚话术，表达理解和重视
3. 暂缓信息收集，优先进行情绪疏导
4. 情绪稳定后，自然过渡到信息收集
5. 若用户持续情绪激动超过 3 轮，提示转接人工坐席

## 话术模板
- "非常理解您的不便和困扰，这确实影响了您的正常生活。我们一定认真记录并跟进您的问题。"
- "您的心情我完全能理解，遇到这种情况确实让人着急。请放心，我会帮您详细记录。"
- "感谢您的耐心反映，我们非常重视每一位市民的诉求。"

## 转人工条件
连续 3 轮检测到强烈负面情绪时：
"我理解您非常着急，为了更好地帮助您，建议您拨打人工热线 12345 转人工服务，会有专人为您处理。"
```

- [ ] **Step 5: Create ticket-submission SKILL.md**

```markdown
---
name: ticket-submission
description: 在投诉要素收集完毕后，格式化并调用工单系统提交，处理提交结果。
---

## 工作流

### 提交前确认
1. 汇总已收集的所有要素
2. 向用户展示摘要并确认："我已为您记录以下信息：[摘要]，是否确认提交？"
3. 用户确认后，调用 submit_ticket 工具

### 提交成功
- 向用户反馈工单编号
- 告知预计处理时间（标准话术："预计 24 小时内反馈处理进展"）

### 提交失败（降级）
- 工单数据已自动保存到本地
- 告知用户："系统繁忙，您的信息已暂存，我们将在系统恢复后立即为您提交，请保留对话记录。"
```

- [ ] **Step 6: Create fallback-handling SKILL.md**

```markdown
---
name: fallback-handling
description: 处理各类异常情况，包括意图无法识别、子智能体超时、API 调用失败等，保证对话流畅性和服务可用性。
---

## 异常类型与处理策略

| 异常类型 | 触发条件 | 处理方式 |
|---------|---------|---------|
| 意图不明确 | 两次追问仍无法分类 | 默认进入咨询流程 |
| 子智能体超时 | 响应 >5s | 告知用户"正在处理"，重试一次 |
| 知识库不可用 | API 返回 5xx 或超时 | 启用大模型兜底，记录错误日志 |
| 工单提交失败 | Ticket API 异常 | 本地保存工单，告知用户稍后重试 |
| 用户情绪激动 | 检测到攻击性语言 | 先安抚，再引导回正题 |
| 对话超时 | 用户超过 5 分钟未回复 | 发送提醒，超 10 分钟关闭会话 |

## 固定应急话术
当主模型服务完全不可用时，返回：
"非常抱歉，当前系统正在维护中，请稍后再试或直接拨打人工热线 12345。给您带来不便，敬请谅解。"
```

- [ ] **Step 7: Create context-switch SKILL.md**

```markdown
---
name: context-switch
description: 检测到对话中途出现意图切换时，负责保存当前任务上下文、切换到新任务、并在新任务完成后恢复原任务。
---

## 触发条件
- 当前有 ACTIVE 任务，用户新输入检测到不同意图

## 工作流

### 暂停当前任务（PAUSE）
1. 调用要素提取子智能体获取当前要素快照
2. 将完整任务状态序列化存入 session stack
3. 更新 active_task.status = "PAUSED"
4. 增加 interrupt_depth 计数
5. 检查 interrupt_depth，超过 2 则拒绝切换

### 恢复暂停任务（RESUME）
1. 新任务完成后，检查 task_stack 中是否有 PAUSED 任务
2. 从 stack 中取出，恢复为 active_task
3. 减少 interrupt_depth 计数
4. 生成自然的过渡话术

## 过渡话术生成规则
- 先给新任务收尾（一句话确认完成）
- 用"我们继续""接下来""回到刚才"等词自然衔接
- 直接给出下一个问题，不重复已收集的信息
```

- [ ] **Step 8: Create multi-complaint SKILL.md**

```markdown
---
name: multi-complaint
description: 处理同一会话中出现多个投诉的情况，负责冲突判断、要素共享、顺序调度和汇总输出。
---

## 触发条件
- 当前有 ACTIVE 投诉任务，用户新输入识别为另一个 COMPLAINT

## 工作流

### 冲突判断
1. 提取两个投诉的地址、时间、类型信息
2. 计算相似度：地址相同且时间相近 → SAME_AREA
3. 否则 → INDEPENDENT

### SAME_AREA 处理
1. 告知用户两个投诉将共享地址等要素
2. 构建共享要素集合，两个工单均引用
3. 分别追问各自独有的要素
4. 分别提交工单，工单中标注"关联投诉"

### INDEPENDENT 处理
1. 将新投诉加入 task_stack，状态 PENDING
2. 先完成当前投诉（current first 策略）
3. 当前工单提交后，自动切换到下一个 PENDING 投诉
4. 每次切换时生成清晰的衔接话术

### 汇总输出
所有投诉完成后，生成汇总确认：
"您本次共提交了 N 项投诉：
  ① [类型]（工单号：XXXX）
  ② [类型]（工单号：XXXX）
  预计 24 小时内跟进，感谢您的耐心！"
```

- [ ] **Step 9: Create warm-response-generation SKILL.md**

```markdown
---
name: warm-response-generation
description: 为建议类诉求生成积极、有温度的正向回应话术。
---

## 工作流
1. 理解用户建议的核心内容
2. 对建议进行分类（城市建设/交通/环境/教育/医疗/其他）
3. 提炼建议要点（一句话概括）
4. 生成回应话术

## 回应模板
- "感谢您的宝贵建议！我们已将您关于{建议要点}的建议记录在案，并将转达至相关部门。您的声音对改善城市服务很有价值。"
- "非常感谢您对{领域}工作的关注和建议！您提出的{建议要点}很有参考价值，我们已做好记录。"

## 分类标签
- 城市建设：公园、道路、基础设施、公共设施
- 交通：公交、地铁、停车、路线
- 环境：绿化、垃圾、空气、噪音治理
- 教育：学校、培训、教育资源
- 医疗：医院、社区卫生、就医流程
- 其他：不属于以上类别
```

- [ ] **Step 10: Commit**

```bash
git add skills/
git commit -m "feat: add all SKILL.md files for agent workflows"
```

---

### Task 7: Main Entry Point — Agent Creation and CLI/Web API

**Files:**
- Create: `main.py`
- Create: `tests/test_agent_creation.py`

- [ ] **Step 1: Write failing test for agent factory**

```python
# tests/test_agent_creation.py
import os
import pytest
from unittest.mock import patch, MagicMock


def test_create_model_returns_chat_model():
    """create_model should return a langchain chat model"""
    with patch("main.init_chat_model") as mock_init:
        mock_init.return_value = MagicMock()
        from main import create_model
        model = create_model()
        mock_init.assert_called_once()
        assert model is not None


def test_create_supervisor_agent_returns_agent():
    """create_supervisor_agent should return a deep agent with correct config"""
    with patch("main.create_deep_agent") as mock_create, \
         patch("main.init_chat_model") as mock_model, \
         patch("main.FilesystemBackend") as mock_backend:
        mock_model.return_value = MagicMock()
        mock_create.return_value = MagicMock()
        mock_backend.return_value = MagicMock()

        from main import create_supervisor_agent
        agent = create_supervisor_agent()
        mock_create.assert_called_once()

        call_kwargs = mock_create.call_args
        # Should have memory pointing to AGENTS.md
        assert any("AGENTS.md" in str(m) for m in call_kwargs.kwargs.get("memory", []) or call_kwargs[1].get("memory", []))


def test_create_complaint_agent_returns_agent():
    """create_complaint_agent should have complaint AGENTS.md and element extraction subagent"""
    with patch("main.create_deep_agent") as mock_create, \
         patch("main.init_chat_model") as mock_model, \
         patch("main.FilesystemBackend") as mock_backend:
        mock_model.return_value = MagicMock()
        mock_create.return_value = MagicMock()
        mock_backend.return_value = MagicMock()

        from main import create_complaint_agent
        agent = create_complaint_agent()
        assert agent is not None
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
python -m pytest tests/test_agent_creation.py -v
```

Expected: FAIL — `ModuleNotFoundError: No module named 'main'`

- [ ] **Step 3: Implement main.py**

```python
# main.py
"""12345 热线智能客服系统入口 — CLI + FastAPI Web API"""

import argparse
import json
import os
import sys
import uuid
from pathlib import Path

from deepagents import create_deep_agent
from deepagents.backends import FilesystemBackend
from dotenv import load_dotenv
from langchain.chat_models import init_chat_model
from rich.console import Console
from rich.panel import Panel

from tools.knowledge_base import query_knowledge_base
from tools.ticket_api import submit_ticket

load_dotenv()

console = Console()

BASE_DIR = Path(__file__).parent
SESSION_DIR = os.getenv("SESSION_DIR", str(BASE_DIR / "sessions"))


# ---------------------------------------------------------------------------
# Model factory
# ---------------------------------------------------------------------------

def create_model():
    """创建 LLM 实例（兼容 OpenAI API 格式的本地模型）"""
    return init_chat_model(
        model=os.getenv("LLM_MODEL_NAME", "qwen"),
        model_provider="openai",
        temperature=0,
        base_url=os.getenv("LLM_BASE_URL", "http://localhost:8000/v1"),
        api_key=os.getenv("LLM_API_KEY", "none"),
        timeout=30,
    )


# ---------------------------------------------------------------------------
# Agent factories
# ---------------------------------------------------------------------------

def create_element_extraction_agent():
    """创建要素提取子智能体 — 分析对话历史，输出要素状态 JSON"""
    model = create_model()
    return create_deep_agent(
        model=model,
        memory=[str(BASE_DIR / "agents" / "element_extraction" / "AGENTS.md")],
        skills=[
            str(BASE_DIR / "skills" / "complaint-type-mapping"),
        ],
        tools=[],
        subagents=[],
        backend=FilesystemBackend(root_dir=SESSION_DIR),
    )


def create_inquiry_agent():
    """创建咨询子智能体 — 知识库查询 + 大模型兜底"""
    model = create_model()
    return create_deep_agent(
        model=model,
        memory=[str(BASE_DIR / "agents" / "inquiry" / "AGENTS.md")],
        skills=[
            str(BASE_DIR / "skills" / "knowledge-base-query"),
        ],
        tools=[query_knowledge_base],
        subagents=[],
        backend=FilesystemBackend(root_dir=SESSION_DIR),
    )


def create_suggestion_agent():
    """创建建议子智能体 — 建议分类 + 温暖回应"""
    model = create_model()
    return create_deep_agent(
        model=model,
        memory=[str(BASE_DIR / "agents" / "suggestion" / "AGENTS.md")],
        skills=[
            str(BASE_DIR / "skills" / "warm-response-generation"),
        ],
        tools=[],
        subagents=[],
        backend=FilesystemBackend(root_dir=SESSION_DIR),
    )


def create_complaint_agent():
    """创建投诉子智能体 — 要素收集 + 工单提交"""
    model = create_model()
    element_agent = create_element_extraction_agent()
    return create_deep_agent(
        model=model,
        memory=[str(BASE_DIR / "agents" / "complaint" / "AGENTS.md")],
        skills=[
            str(BASE_DIR / "skills" / "complaint-type-mapping"),
            str(BASE_DIR / "skills" / "empathy-response"),
            str(BASE_DIR / "skills" / "ticket-submission"),
        ],
        tools=[query_knowledge_base, submit_ticket],
        subagents=[element_agent],
        backend=FilesystemBackend(root_dir=SESSION_DIR),
    )


def create_supervisor_agent():
    """创建主智能体（Supervisor）— 意图分类 + 路由调度"""
    model = create_model()

    complaint_agent = create_complaint_agent()
    inquiry_agent = create_inquiry_agent()
    suggestion_agent = create_suggestion_agent()

    return create_deep_agent(
        model=model,
        memory=[str(BASE_DIR / "AGENTS.md")],
        skills=[
            str(BASE_DIR / "skills" / "intent-classification"),
            str(BASE_DIR / "skills" / "fallback-handling"),
            str(BASE_DIR / "skills" / "context-switch"),
            str(BASE_DIR / "skills" / "multi-complaint"),
        ],
        tools=[query_knowledge_base, submit_ticket],
        subagents=[complaint_agent, inquiry_agent, suggestion_agent],
        backend=FilesystemBackend(root_dir=SESSION_DIR),
    )


# ---------------------------------------------------------------------------
# CLI mode
# ---------------------------------------------------------------------------

def run_cli():
    """命令行交互模式"""
    parser = argparse.ArgumentParser(
        description="12345 热线智能客服系统",
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog="""
示例:
  python main.py --message "我家停水了"
  python main.py --interactive
  python main.py --serve
        """,
    )
    parser.add_argument("--message", type=str, help="单次消息模式")
    parser.add_argument("--interactive", action="store_true", help="交互式对话模式")
    parser.add_argument("--serve", action="store_true", help="启动 Web API 服务")
    parser.add_argument("--host", type=str, default="0.0.0.0", help="Web API 监听地址")
    parser.add_argument("--port", type=int, default=8080, help="Web API 监听端口")

    args = parser.parse_args()

    if args.serve:
        import uvicorn
        from web import app
        uvicorn.run(app, host=args.host, port=args.port)
        return

    console.print(Panel("12345 热线智能客服系统", style="bold blue"))
    console.print("[dim]正在初始化智能体...[/dim]")
    agent = create_supervisor_agent()
    console.print("[green]初始化完成！[/green]\n")

    if args.message:
        # 单次消息模式
        _process_single(agent, args.message)
    elif args.interactive:
        # 交互式对话模式
        _run_interactive(agent)
    else:
        # 默认交互式
        _run_interactive(agent)


def _process_single(agent, message: str):
    """处理单条消息"""
    console.print(f"[bold cyan]用户:[/bold cyan] {message}")
    try:
        result = agent.invoke({"messages": [{"role": "user", "content": message}]})
        answer = result["messages"][-1]
        content = answer.content if hasattr(answer, "content") else str(answer)
        console.print(Panel(f"[bold green]客服:[/bold green]\n\n{content}", border_style="green"))
    except Exception as e:
        console.print(Panel(f"[bold red]错误:[/bold red] {str(e)}", border_style="red"))
        sys.exit(1)


def _run_interactive(agent):
    """交互式多轮对话"""
    console.print("[dim]输入 'quit' 或 'exit' 退出对话[/dim]\n")
    messages = []

    while True:
        try:
            user_input = console.input("[bold cyan]用户> [/bold cyan]")
        except (EOFError, KeyboardInterrupt):
            break

        if user_input.strip().lower() in ("quit", "exit", "q"):
            console.print("\n[dim]感谢使用 12345 热线智能客服，祝您生活愉快！[/dim]")
            break

        if not user_input.strip():
            continue

        messages.append({"role": "user", "content": user_input})

        try:
            result = agent.invoke({"messages": messages})
            answer = result["messages"][-1]
            content = answer.content if hasattr(answer, "content") else str(answer)
            console.print(f"\n[bold green]客服>[/bold green] {content}\n")
            messages.append({"role": "assistant", "content": content})
        except Exception as e:
            console.print(f"\n[bold red]系统错误:[/bold red] {str(e)}\n")
            console.print("[dim]非常抱歉，系统遇到了一些问题。请稍后再试或拨打人工热线 12345。[/dim]\n")


# ---------------------------------------------------------------------------
# Web API mode (FastAPI)
# ---------------------------------------------------------------------------

def create_web_app():
    """创建 FastAPI 应用"""
    from fastapi import FastAPI
    from pydantic import BaseModel

    app = FastAPI(title="12345 热线智能客服 API", version="0.1.0")

    # 会话级 agent 缓存
    _agents: dict = {}
    _conversations: dict = {}

    class ChatRequest(BaseModel):
        session_id: str = ""
        message: str

    class ChatResponse(BaseModel):
        session_id: str
        reply: str
        intent: str = ""
        error: str = ""

    def _get_or_create_agent(session_id: str):
        if session_id not in _agents:
            _agents[session_id] = create_supervisor_agent()
            _conversations[session_id] = []
        return _agents[session_id], _conversations[session_id]

    @app.post("/api/chat", response_model=ChatResponse)
    def chat(req: ChatRequest):
        session_id = req.session_id or str(uuid.uuid4())
        agent, messages = _get_or_create_agent(session_id)
        messages.append({"role": "user", "content": req.message})

        try:
            result = agent.invoke({"messages": messages})
            answer = result["messages"][-1]
            content = answer.content if hasattr(answer, "content") else str(answer)
            messages.append({"role": "assistant", "content": content})
            return ChatResponse(session_id=session_id, reply=content)
        except Exception as e:
            return ChatResponse(
                session_id=session_id,
                reply="非常抱歉，当前系统正在维护中，请稍后再试或直接拨打人工热线 12345。",
                error=str(e),
            )

    @app.get("/health")
    def health():
        return {"status": "ok"}

    return app


# Lazy-create for `uvicorn main:app`
app = None


def _get_app():
    global app
    if app is None:
        app = create_web_app()
    return app


# Allow `uvicorn main:app` by making app a lazy property
def __getattr__(name):
    if name == "app":
        return _get_app()
    raise AttributeError(name)


if __name__ == "__main__":
    run_cli()
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
python -m pytest tests/test_agent_creation.py -v
```

Expected: All PASS

- [ ] **Step 5: Commit**

```bash
git add main.py tests/test_agent_creation.py
git commit -m "feat: add main.py with supervisor agent, CLI interactive mode, and FastAPI web API"
```

---

### Task 8: Integration Smoke Test

**Files:**
- No new files, just verification

- [ ] **Step 1: Run full test suite**

```bash
cd /home/zhengxin/Documents/workspace/12345-hotline-agent
python -m pytest tests/ -v
```

Expected: All tests pass

- [ ] **Step 2: Verify CLI help text**

```bash
python main.py --help
```

Expected: Shows usage with `--message`, `--interactive`, `--serve` options

- [ ] **Step 3: Verify all AGENTS.md and SKILL.md files exist**

```bash
find . -name "AGENTS.md" -o -name "SKILL.md" | sort
```

Expected output:
```
./AGENTS.md
./agents/complaint/AGENTS.md
./agents/element_extraction/AGENTS.md
./agents/inquiry/AGENTS.md
./agents/suggestion/AGENTS.md
./skills/complaint-type-mapping/SKILL.md
./skills/context-switch/SKILL.md
./skills/empathy-response/SKILL.md
./skills/fallback-handling/SKILL.md
./skills/intent-classification/SKILL.md
./skills/knowledge-base-query/SKILL.md
./skills/multi-complaint/SKILL.md
./skills/ticket-submission/SKILL.md
./skills/warm-response-generation/SKILL.md
```

- [ ] **Step 4: Verify data files are valid JSON**

```bash
python -c "import json; json.load(open('data/complaint_schemas.json')); json.load(open('data/intent_keywords.json')); print('OK')"
```

Expected: `OK`

- [ ] **Step 5: Final commit with all remaining files**

```bash
git add -A
git status
# If there are unstaged new directories (sessions/, logs/), add .gitkeep:
touch sessions/.gitkeep logs/.gitkeep
git add sessions/.gitkeep logs/.gitkeep
git commit -m "chore: add runtime directories and verify project structure"
```

---

## Summary of Deliverables

| Component | Files | Purpose |
|-----------|-------|---------|
| Project scaffold | `pyproject.toml`, `.env.example` | Dependencies, env config |
| Data | `data/complaint_schemas.json`, `data/intent_keywords.json` | Complaint element schemas, intent keywords |
| Session store | `tools/session_store.py` | Multi-task session stack with persistence |
| API tools | `tools/knowledge_base.py`, `tools/ticket_api.py` | Knowledge base + ticket system clients |
| Supervisor agent | `AGENTS.md` + 4 skills | Intent classification, routing, context switching |
| Complaint agent | `agents/complaint/AGENTS.md` + 3 skills | Complaint handling, element collection |
| Element extraction | `agents/element_extraction/AGENTS.md` | Structured element analysis (JSON output) |
| Inquiry agent | `agents/inquiry/AGENTS.md` + 1 skill | Knowledge base query with fallback |
| Suggestion agent | `agents/suggestion/AGENTS.md` + 1 skill | Warm response generation |
| Entry point | `main.py` | CLI (single/interactive) + FastAPI web API |
| Tests | `tests/test_session_store.py`, `tests/test_tools.py`, `tests/test_agent_creation.py` | Unit tests for all components |
